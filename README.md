# nxs stable runtime channel

This tag is intentionally detached from source branches.

The stable release contains nxs-manifest.json, a compatibility index of runtime releases.
Bridge clients choose the newest runtime whose min_bridge_version is compatible with the linked bridge module.

Last updated from runtime version: 0.1.6
